"# Test Credentials
# Agent writes here when creating/modifying auth credentials (admin accounts, test users).
# Testing agent reads this before auth tests. Fork/continuation agents read on startup." --new-str "# Test Credentials

## Admin Dashboard (Diamond Chem India)
- URL: `/admin/login`
- Username: `admin`
- Password: `admin123`
"